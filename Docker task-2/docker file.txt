FROM nginx:alpine

# Copy the HTML file into nginx's default web directory
COPY index.html /usr/share/nginx/html/index.html

# Expose port 80
EXPOSE 80

# Nginx starts automatically; no CMD needed